🚀 API 文档专用提取提示词（Markdown 版）
提示词内容：

请深度阅读并分析这个 API 页面：[在此粘贴 URL]

任务目标： 将该页面的 API 信息整理为 Markdown 格式的代码块。为了保证我本地文档的纯净度和完整性，请严格执行以下规则：

1. 来源禁令（重点）：

仅限当前页面：严禁搜索任何第三方 SDK、GitHub 源码或镜像站点。只准整理我提供的这个 URL 页面上能看到的信息。

2. 核心字段（仅保留这 5 项）：

接口名称

请求方法（GET/POST 等）

Endpoint（完整路径）

请求参数（需包含：字段名、类型、是否必填、说明）

返回参数（需包含：字段名、类型、说明）

3. 完整性死命令：

禁止省略：无论返回参数有多少个字段，必须全部罗列。严禁出现“...”、“此处省略”或“等等”字样。

层级清晰：如果有嵌套结构，请在字段名前加 — 符号或缩进来体现父子关系。

数据严谨：追求的是100% 还原接口、零歧义

4. 输出要求：

必须将整理好的 Markdown 内容放在一个 代码块 中，方便我一键复制。

请开始你的表演。